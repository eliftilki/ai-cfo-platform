# Shared Package

This package is reserved for cross-service constants, prompt fragments and small utilities that are shared by both Python services.

Current MVP code keeps the active request/response models in `packages/contracts/python`.
